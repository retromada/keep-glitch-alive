{
  "name": "keep-glitch-alive",
  "version": "0.1.0",
  "description": "Keep multiple applications hosted on the glitch always alive",
  "author": "retromada",
  "repository": {
    "type": "git",
    "url": "https://github.com/retromada/keep-glitch-alive.git"
  },
  "engines": {
    "node": ">=12"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rimraf dist && tsc",
    "watch": "tsc --watch",
    "prepare": "npm run build"
  },
  "dependencies": {
    "express": "^4.17.1",
    "node-fetch": "^2.6.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.6",
    "@types/node": "^14.0.9",
    "@types/node-fetch": "^2.5.7",
    "dotenv": "^8.2.0",
    "rimraf": "^3.0.2",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.3"
  },
  "license": "MIT"
}
